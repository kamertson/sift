/**
 * Add two numbers.
 */
function add(a, b) {
  return a + b;
}

const square = (value) => {
  return value * value;
};

class Greeter {
  /**
   * Say hello.
   */
  greet(name) {
    return `Hello, ${name}!`;
  }
}
