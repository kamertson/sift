/**
 * Double a number.
 */
function double(value: number): number {
  return value * 2;
}

class Counter {
  /**
   * Increment the count.
   */
  increment(step: number): number {
    return step + 1;
  }
}
